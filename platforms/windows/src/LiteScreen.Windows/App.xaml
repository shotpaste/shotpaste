<Application x:Class="LiteScreen.Windows.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             ShutdownMode="OnExplicitShutdown">
  <Application.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Resources/AnnotationIcons.xaml" />
      </ResourceDictionary.MergedDictionaries>
    <SolidColorBrush x:Key="AccentBrush" Color="#7C3AED" />
    <SolidColorBrush x:Key="AccentSoftBrush" Color="#187C3AED" />
    <SolidColorBrush x:Key="WindowBrush" Color="#F7F7FA" />
    <SolidColorBrush x:Key="SurfaceBrush" Color="#FFFFFFFF" />
    <SolidColorBrush x:Key="SurfaceSecondaryBrush" Color="#FFF1F1F5" />
    <SolidColorBrush x:Key="TextBrush" Color="#FF19191D" />
    <SolidColorBrush x:Key="SecondaryTextBrush" Color="#FF71717A" />
    <SolidColorBrush x:Key="BorderBrush" Color="#1F000000" />
    <SolidColorBrush x:Key="HudBrush" Color="#EE24242A" />
    <SolidColorBrush x:Key="HudBorderBrush" Color="#38FFFFFF" />
    <SolidColorBrush x:Key="HudHoverBrush" Color="#1FFFFFFF" />
    <SolidColorBrush x:Key="HudSelectedBrush" Color="#26FFFFFF" />
    <SolidColorBrush x:Key="HudTextBrush" Color="#FFF4F4F5" />
    <SolidColorBrush x:Key="HudSecondaryTextBrush" Color="#FFB4B4BD" />
    <SolidColorBrush x:Key="DangerBrush" Color="#FFFF5A57" />

    <Style TargetType="Window">
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="Icon" Value="pack://application:,,,/Assets/lite-screen-icon.png" />
      <Setter Property="Background" Value="{DynamicResource WindowBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>
    <Style TargetType="Button">
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      <Setter Property="Padding" Value="14,7" />
      <Setter Property="MinHeight" Value="32" />
      <Setter Property="Background" Value="{DynamicResource SurfaceSecondaryBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
      <Setter Property="BorderThickness" Value="1" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" CornerRadius="7" Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.82" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.64" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Chrome" Property="Opacity" Value="0.4" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
      <Setter Property="Background" Value="{DynamicResource AccentBrush}" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderThickness" Value="0" />
    </Style>
    <Style x:Key="HudIconButton" TargetType="Button">
      <Setter Property="Width" Value="34" />
      <Setter Property="Height" Value="34" />
      <Setter Property="Padding" Value="0" />
      <Setter Property="FontFamily" Value="Segoe UI Symbol, Segoe UI" />
      <Setter Property="FontSize" Value="17" />
      <Setter Property="Foreground" Value="{DynamicResource HudTextBrush}" />
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Cursor" Value="Hand" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Chrome" CornerRadius="7" Background="{TemplateBinding Background}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Chrome" Property="Background" Value="{DynamicResource HudHoverBrush}" /></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Chrome" Property="Opacity" Value="0.65" /></Trigger>
              <Trigger Property="IsEnabled" Value="False"><Setter TargetName="Chrome" Property="Opacity" Value="0.32" /></Trigger>
              <Trigger Property="Tag" Value="Selected"><Setter TargetName="Chrome" Property="Background" Value="{DynamicResource HudSelectedBrush}" /></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
    <Style x:Key="HudTextButton" TargetType="Button" BasedOn="{StaticResource HudIconButton}">
      <Setter Property="Width" Value="Auto" />
      <Setter Property="Padding" Value="10,0" />
      <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI" />
      <Setter Property="FontSize" Value="12" />
      <Setter Property="FontWeight" Value="SemiBold" />
    </Style>
    <Style x:Key="HudSlider" TargetType="Slider">
      <Setter Property="Foreground" Value="{DynamicResource HudTextBrush}" />
      <Setter Property="VerticalAlignment" Value="Center" />
    </Style>
    <Style TargetType="ToolTip">
      <Setter Property="Background" Value="#F51D1D21" />
      <Setter Property="Foreground" Value="White" />
      <Setter Property="BorderBrush" Value="#32FFFFFF" />
      <Setter Property="Padding" Value="9,6" />
      <Setter Property="FontSize" Value="11" />
    </Style>
    <Style TargetType="TextBox">
      <Setter Property="Padding" Value="10,7" />
      <Setter Property="Background" Value="{DynamicResource SurfaceBrush}" />
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
      <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}" />
    </Style>
    <Style TargetType="CheckBox">
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>
    <Style TargetType="RadioButton">
      <Setter Property="Foreground" Value="{DynamicResource TextBrush}" />
    </Style>
    <Style TargetType="ComboBox">
      <Setter Property="Foreground" Value="#FF19191D" />
    </Style>
    <Style TargetType="ComboBoxItem">
      <Setter Property="Foreground" Value="#FF19191D" />
    </Style>
    </ResourceDictionary>
  </Application.Resources>
</Application>
