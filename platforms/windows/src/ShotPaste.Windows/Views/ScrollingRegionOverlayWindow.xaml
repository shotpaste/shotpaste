<Window x:Class="ShotPaste.Windows.Views.ScrollingRegionOverlayWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:loc="clr-namespace:ShotPaste.Windows.Services"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{loc:Localized Key='ShotPaste · 滚动截屏选区', IsWindowTitle=True}" WindowStyle="None" ResizeMode="NoResize"
        AllowsTransparency="True" Background="Transparent" ShowInTaskbar="False"
        ShowActivated="False" Focusable="False" IsHitTestVisible="False" Topmost="True">
  <Canvas x:Name="OverlayCanvas">
    <Rectangle x:Name="TopLine" Fill="{DynamicResource AccentBrush}" SnapsToDevicePixels="True"/>
    <Rectangle x:Name="RightLine" Fill="{DynamicResource AccentBrush}" SnapsToDevicePixels="True"/>
    <Rectangle x:Name="BottomLine" Fill="{DynamicResource AccentBrush}" SnapsToDevicePixels="True"/>
    <Rectangle x:Name="LeftLine" Fill="{DynamicResource AccentBrush}" SnapsToDevicePixels="True"/>
    <Border x:Name="StatusBadge" CornerRadius="{DynamicResource Radius.Lg}" Background="{DynamicResource HudBrush}" BorderBrush="{DynamicResource HudBorderBrush}"
            BorderThickness="1" Padding="10,6">
      <StackPanel Orientation="Horizontal">
        <Ellipse Width="7" Height="7" Fill="{DynamicResource AccentBrush}" Margin="0,0,7,0" VerticalAlignment="Center"/>
        <TextBlock Text="{loc:Localized Key='滚动捕获区域'}" Foreground="{DynamicResource HudTextBrush}" FontSize="11" FontWeight="SemiBold"/>
      </StackPanel>
    </Border>
  </Canvas>
</Window>
